import React from "react";

const Footer = ()=>{ 
     
    return (
        <div>
            <button>All</button>
            <button>Completed</button>
            <button>Active</button>
        </div>
    )
}

export default Footer;